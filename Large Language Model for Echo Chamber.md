## 論文2 執行細節完整性評估

### 有說明的部分

| 段落 | 內容 | 完整性 |
|---|---|---|
| Section 3.3 | f 和 g 函數由 LLM 輸出，輸入為 $C_i$ 和 $C_j$ | ⚠️ 部分 |
| Section 3.3 | $y_i(t)$ 的內容生成公式 | ✅ 清楚 |
| Section 5 | $T_g$ 的 prompt 示例（內容生成） | ✅ 清楚 |
| Section 5 | $T_o$ 和 $T_r$ 可透過調整 $T_g$ 得到 | ⚠️ 部分 |

---

### 未說明的部分

論文2 在以下關鍵執行細節上**未提供明確說明**：

1. **$T_o$ 的 prompt 示例缺失**
   - $T_o$ 對應影響函數 $f$，其 LLM output 應為意見更新的影響量
   - 論文未提供對應 prompt，輸出格式不明

2. **$T_r$ 的 prompt 示例缺失**
   - $T_r$ 對應一致性函數 $g$，其 LLM output 需為 $0 \leq g \leq 1$ 的數值，才能代入 Follow / Unfollow 機率公式
   - 論文未說明 LLM 如何被要求輸出此範圍內的數值

3. **文字語意轉數值的方法未說明**
   - 論文未解釋 LLM 生成的文字內容，如何被轉換為可代入公式的數值
   - 例如：是直接要求 LLM 輸出數字？還是透過 embedding 計算語意相似度？論文皆未提及

4. **Stance 萃取方法未說明**
   - Section 7.2 提到 Opinion Accuracy 的評估，但未說明如何從 LLM 生成的文字中萃取 stance 數值

---

### 結論

> 論文2 **有提到**文字語意轉數值的架構（Section 3.3），但**未完整說明**其執行細節。
> 尤其是 $T_o$ 和 $T_r$ 的 prompt 設計，以及 LLM output 如何轉換為可用數值，
> 論文僅提供 $T_g$ 的示例作為參考，對於其餘兩個範本的具體實作方式，
> 屬於論文在**可重現性（Reproducibility）上的明顯缺口**。
